namespace Polaby.Repositories.Enums
{
    public enum PostVisibility
    {
        Public, // Công khai
        Private, // Riêng tư
        Restricted // Hạn chế (chỉ cho một nhóm người dùng cụ thể)
    }
}